# Codtech-Java-Internship
Task-1 focuses on creating a Java program that performs basic file handling operations. The program creates a text file, writes data into it, reads the content, and modifies the file by appending new data. This task demonstrates the use of Java I/O classes and exception handling to work with files efficiently.
